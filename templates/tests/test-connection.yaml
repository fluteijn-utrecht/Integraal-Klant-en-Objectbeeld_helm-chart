apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "iko.fullname" . }}-test-connection"
  labels:
    {{- include "iko.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "iko.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never